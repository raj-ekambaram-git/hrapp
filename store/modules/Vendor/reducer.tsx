
const initialState = {
    accountId: 0,
    authorized: false,
    role: "USER",
    vendorId: 1
};

const vendorReducer = (state = initialState, {type, payload}) => {

};

export default vendorReducer;